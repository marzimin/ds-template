## Summary

<!-- What does this PR do? Why is it needed? -->

## Changes

-

## Testing

- [ ] Tests added / updated
- [ ] `make test` passes locally (both suites)
- [ ] `make lint` passes locally
- [ ] `make types` re-run and both generated files committed (if the API changed)
- [ ] Ran the pipeline end to end (if the ML pipeline or config changed)

## Notes

<!-- Any context reviewers should know (e.g. config changes, new dependencies,
     anything that affects the hand-off from de-template — see docs/handoff.md) -->
